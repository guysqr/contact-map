version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - yarn lint
    build:
      commands:
        - yarn build
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: vue-contact-map/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
